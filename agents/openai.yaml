interface:
  display_name: "Interaction Flow Kit"
  short_description: "Design usable flows and implementation-ready systems"
  default_prompt: "Use $interaction-flow-kit to infer the real user job behind this request and design it from PRD through technical implementation, closing the path from result to next useful action and making any cross-stage data handoff explicit."

policy:
  allow_implicit_invocation: true
