interface:
  display_name: "Interaction Flow Kit"
  short_description: "Turn feature requests into complete user outcomes"
  default_prompt: "Use $interaction-flow-kit to turn this feature request into a complete user outcome and an implementation-ready interaction contract."

policy:
  allow_implicit_invocation: true
