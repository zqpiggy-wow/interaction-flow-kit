## Problem

What real request, Agent outcome, or CLI behavior motivated this change?

## Change

What decision or observable behavior changes? Keep the scope focused.

## Verification

- [ ] `npm run check` passes
- [ ] `npm pack --dry-run` contains the intended package files
- [ ] New guidance is proportional and does not require unnecessary artifacts
- [ ] Documentation and tests are updated when behavior changes
